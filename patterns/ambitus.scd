(
		//ambitus
		// \degree, p{ arg ev;
		// 	var offset=0;
		// 	var min=(-5), max=10;
		// 	loop{
		// 		if(min(ev.degree,min)==ev.degree, {
		// 			offset=offset-1
		// 		});
		// 		if(max(ev.degree,max)==ev.degree, {
		// 			offset=offset+1
		// 		});
		// 		ev=(ev.degree+(offset*2)).postln.yield
		// 	}
		// },
		//Pkey(\degree).wrap(-5,12),

)